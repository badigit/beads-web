# Скопируй в .dolt.env (gitignored) или положи BEADS_DOLT_PASSWORD в shell env.
# Предпочтительный способ — общий credentials-файл, а не этот legacy-путь:
# %APPDATA%\beads\credentials (Windows) / ~/.config/beads/credentials.
BEADS_DOLT_PASSWORD=
